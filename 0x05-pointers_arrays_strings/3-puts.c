#include "main.h"
/**
 * _puts - the function that prints a string, followed by
 *  a new line, to stdou
 *  @str: the pointer to string characters
 *  Return: 0
 */
void _puts(char *str)
{
	for (; *str != '\0'; s++)
	{
		_putchar(*s);
	}
	_putchar('\n');
}

#include "main.h"
/**
 * puts2 - the  function that prints every other character of a string,
 * starting with the first character, followed by a new line
 * @str: the pointer of character string
 * Return: void
 */
void puts2(char *str)
{
	for (; *s != '\0'; )
	{
		-putchar(*s);
		s += 2;
	}
	_putchar('\n');
}

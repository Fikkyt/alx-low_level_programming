#include <stdio.h>
#include "main.h"
/**
 * main - the main program
 * @argc: the argument count
 * @argv: the argument vector
 * Return: 0
 */
int main(int argc, char *argv[])
{
	printf("%d\n", (argc - 1));
	return (0);
}

#include <stdio.h>
#include "main.h"
/**
 * main - the main program
 * @argc: the argument count
 * @argv: the argument vector
 * Return: 0
 */
int main(int argc, char *argv[])
{
	int i;

	for (i = 0; i < argc; i++)
	{
		;
	}
	printf("%d\n", (i - 1));
	return (0);
}

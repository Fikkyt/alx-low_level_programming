#include <stdio.h>
/**
 * main -  entry point
 * The programs print a specified statement using the printf()
 * Return: 0
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art, ");
	return (0);
}

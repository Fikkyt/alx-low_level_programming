#include "main.h"
/**
 * add - the entry point
 * Description: the functiin adds two integers
 * @m: the integer 1
 * @n: the integer 2
 * Return: result
 */
int add(int m, int n)
{
	int result;

	result = m + n;
	return (result);
}

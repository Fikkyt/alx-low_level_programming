#include <stdio.h>
/**
 * main - the starting point
 * The programme prints hexadecimal
 * Return: 0
*/
int main(void)
{
	int m, k;

	for (m == 0; m <= 9; m++)
	{
		putchar(m);
	}
	for (k == 'a'; k <= 'f'; k++)
	{
		putchar(k);
	}
	putchar('\n');
	return (0);
}

#include <stdio.h>
/**
 * main - entry point
 * The programme gives random out and print out the result using if statement
 * Return: 0
 */
int main(void)
{
	char m;

	m = 'a';
	while (m <= 'z')
	{
		putchar(m);
		m++
	}
	putchar('\n');
}
